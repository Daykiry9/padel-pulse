{
  "name": "@padel-pulse/supabase",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "gen:types": "supabase gen types typescript --linked > src/database.types.ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.2",
    "@supabase/supabase-js": "^2.46.1"
  },
  "devDependencies": {
    "@padel-pulse/tsconfig": "workspace:*",
    "typescript": "^5.6.3"
  }
}
