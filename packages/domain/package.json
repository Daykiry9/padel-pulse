{
  "name": "@padelking/domain",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@padelking/tsconfig": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
