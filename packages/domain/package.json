{
  "name": "@padel-pulse/domain",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@padel-pulse/tsconfig": "workspace:*",
    "typescript": "^5.6.3"
  }
}
